<nav class="navbar">
  <a href="/" class="nav-logo">Mein Ausflug</a>
  <div class="hamburger">
    <span class="hamburger-line"></span>
    <span class="hamburger-line"></span>
    <span class="hamburger-line"></span>
  </div>
  <div class="nav-links">
    <a href="/destinations" class="nav-link">Destinations</a>
    <a href="/hotels" class="nav-link">Hotels</a>
    {% if app.session.get('user') and app.session.get('user').role == 'admin' %}
      <div class="admin-dropdown">
        <button class="nav-link admin-dropdown-btn">
          Admin
          <i class="fas fa-chevron-down"></i>
        </button>
        <div class="admin-nav">
          <a href="{{ path('app_admin_destinations') }}" class="nav-link">
            <i class="fas fa-map-marker-alt"></i>
            Destinations
          </a>
          <a href="{{ path('app_admin_hotels') }}" class="nav-link">
            <i class="fas fa-hotel"></i>
            Hotels
          </a>
          <a href="{{ path('app_admin_users') }}" class="nav-link">
            <i class="fas fa-users"></i>
            Users
          </a>
        </div>
      </div>
    {% endif %}
    {% if app.session.get('user') %}
      <a href="/profile" class="nav-link">Profile</a>
      <a href="{{ path('app_signout') }}" class="nav-link">Sign Out</a>
    {% else %}
      <a href="/signin" class="nav-link nav-button">Sign In</a>
    {% endif %}
  </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const hamburger = document.querySelector('.hamburger');
    const navLinks = document.querySelector('.nav-links');
    
    hamburger.addEventListener('click', function() {
        navLinks.classList.toggle('active');
    });
});
</script> 